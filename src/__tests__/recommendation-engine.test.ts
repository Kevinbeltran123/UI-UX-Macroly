// Tests for src/domain/recommendation/recommendation-engine.ts — PERIOD-06
// VALIDATION.md: task 1-W0-09

describe("recommend() — period-scaled goals (PERIOD-06)", () => {
  it.todo("returns top N products sorted by gap score");
  it.todo("with scaled goals (protein=300) scores differently than unscaled (protein=150)");
  it.todo("returns empty array when products array is empty");
  it.todo("includes a reason string in each RecommendedProduct");
});
