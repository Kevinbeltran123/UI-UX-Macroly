// Tests for src/domain/catalog/compatibility.ts — PERIOD-05
// VALIDATION.md: task 1-W0-08

describe("checkCompatibility — period-scaled goals (PERIOD-05)", () => {
  it.todo("returns 'fits' when totals + product <= scaledGoals");
  it.todo("returns 'tight' when result > 90% of scaledGoal");
  it.todo("returns 'exceeds' with scaled goals where unscaled would return 'fits'");
  it.todo("exceededMacros lists the correct macro names");
});
